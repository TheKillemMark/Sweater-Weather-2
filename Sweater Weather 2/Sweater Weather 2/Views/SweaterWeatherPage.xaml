<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:viewmodels="clr-namespace:Sweater_Weather_2.ViewModels"
             x:Class="Sweater_Weather_2.Views.SweaterWeatherPage"
             BackgroundColor="SkyBlue"
             x:DataType="viewmodels:SweaterWeatherViewModel">
    
    <ContentPage.BindingContext>
        <viewmodels:SweaterWeatherViewModel/>
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <Grid RowDefinitions="*,Auto,Auto,*">
            <Label 
                Grid.Row ="1"
                HorizontalOptions="Center"
                FontSize="Large"
                Text="{Binding CountDisplay}"/>
            <Button 
                    Command ="{Binding IncreaseCount}"
                    Grid.Row ="2" Text="Click Me"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>